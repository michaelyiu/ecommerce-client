import {
	makeStyles,
} from '@material-ui/core';

export const useStyles = makeStyles(theme => ({
	table: {
		minWidth: 50,
	},
	banner: {
		backgroundColor: 'rgb(46, 59, 85)',
		color: 'white',
		width: '100%'
	}
}));